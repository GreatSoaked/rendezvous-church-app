import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rendezvouschurch.app',
  appName: 'rendezvouschurch',
  webDir: 'public'
};

export default config;
